ceFile').files?.[0];if(!file){clearAttachment();return;}const epoch=++uploadEpoch;uploading=true;attachmentTokens=[];lastAttachment=null;updateTray();$('#checkForm button[type=submit]').disabled=true;const st=$('#uploadStatus');st.hidden=false;st.className='upload-status';st.textContent='Đang gửi ảnh đến máy chủ demo để kiểm tra…';const fd=new FormData();fd.append('file',file);try{const res=await ui12Fetch('upload','/api/v1/uploads/image',{method:'POST',body:fd},20000);const data=await res.json().catch(()=>({}));if(!res.ok)throw new Error(data.detail||'Không thể tiếp nhận ảnh');if(epoch!==uploadEpoch)return;lastAttachment=data.attachment;attachmentTokens=[data.attachment.attachment_token];st.classList.add(lastAttachment.state==='QR_DECODED'?'good':'warn');st.innerHTML=lastAttachment.state==='QR_DECODED'?`<strong>Đã nhận tệp và đọc được QR.</strong> Loại nội dung: ${escapeHtml(lastAttachment.qr_payload_type||'UNKNOWN')}. QR decode không phải xác minh giao dịch hay độ an toàn của nội dung.`:`<strong>Đã nhận tệp.</strong> Demo chưa đọc được chữ trong ảnh này; ảnh không được dùng như bằng chứng nghiệp vụ ngoài metadata kỹ thuật. Nếu có nội dung cần kiểm tra, hãy nhập bằng chữ.`;updateTray();}catch(e){if(epoch!==uploadEpoch)return;attachmentTokens=[];lastAttachment=null;st.classList.add('bad');st.textContent=e.message||'Không thể tiếp nhận ảnh. Bạn có thể thử lại, bỏ ảnh hoặc nhập nội dung bằng tay.';updateTray();}finally{if(epoch===uploadEpoch){uploading=false;$('#checkForm button[type=submit]').disabled=false;}}}
async function ui12Submit(e){e.preventDefault();ui12Inline('#formError');if(uploading){ui12Inline('#formError','Vui lòng chờ xử lý ảnh hoàn tất hoặc bỏ ảnh trước khi kiểm tra.');return;}const epoch=caseEpoch;const input={situation:$('#situation').value,phone:$('#phone').value||null,account:$('#account').value||null,action_state:$('#actionState').value,attachment_tokens:[...attachmentTokens],exposure_answers:ui12Exposure()};const b=e.submitter;b.disabled=true;b.textContent='Đang đối chiếu…';try{const res=await ui12Fetch('check','/api/v1/check',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(input)},18000);const data=await res.json().catch(()=>({}));if(!res.ok)throw new Error(data.detail||'Không thể xử lý yêu cầu');if(epoch!==caseEpoch)return;submittedInput=input;submittedCaseId=data.case_id;lastResult=data;localStorage.setItem(LAST_CASE_KEY,data.case_id);renderResult(data);show('#result');updateResumeCard();}catch(err){if(epoch===caseEpoch)ui12Inline('#formError',err.message||'Có lỗi xảy ra. Dữ liệu trong form vẫn được giữ để bạn thử lại.');}finally{b.disabled=false;b.textContent='Kiểm tra tình huống';}}

resetIntake=function(){caseEpoch++;['check','clarify','evidence','delete'].forEach(ui12Cancel);ui12Inline('#formError');ui12Inline('#resultStatus');submittedInput=null;submittedCaseId=null;lastResult=null;$('#checkForm')?.reset();clearAttachment();updateCount();$('#situation').placeholder='Mô tả yêu cầu bạn nhận được và việc bạn đã làm. Không nhập mã hoặc mật khẩu thật.';};
loadProfile=function(id){const p=profiles.find(x=>x.id===id);if(!p)return;resetIntake();show('#intake');$('#situation').value=p.situation;$('#phone').value=p.phone||'';$('#account').value=p.account||'';$('#actionState').value=p.action_state||'UNKNOWN';$('#credentialExposure').value='UNKNOWN';$('#controlExposure').value='UNKNOWN';updateTray();updateCount();toast(`Đã điền ${p.label}`);};

renderResult=function(r){ui12Inline('#result