ions?`<p><small>Giới hạn: ${escapeHtml(e.limitations)}</small></p>`:''}${e.source_id&&sm[e.source_id]?`<div class="source-card"><small>${escapeHtml(sm[e.source_id].authority_class)} · ${escapeHtml(sm[e.source_id].published_at)}</small><br>${sm[e.source_id].canonical_url?`<a href="${escapeHtml(safeUrl(sm[e.source_id].canonical_url))}" target="_blank" rel="noopener">${escapeHtml(sm[e.source_id].title)} ↗</a>`:`<strong>${escapeHtml(sm[e.source_id].title)}</strong>`}<p>${escapeHtml(sm[e.source_id].limitations||'')}</p></div>`:''}</div>`;}).join('')}<div class="trace"><b>TRACE</b><br>${Object.entries(data.trace||{}).map(([k,v])=>`${escapeHtml(k)}=${escapeHtml(v)}`).join('<br>')}</div></div>`;$('#evidenceDialog').showModal();}catch(e){if(epoch===caseEpoch)ui12Inline('#resultStatus',`${e.message||'Không tải được nguồn.'} Kết quả hiện tại vẫn giữ nguyên; bạn có thể thử lại.`);}};
deleteCurrentCase=async function(){if(!lastResult)return;ui12Inline('#resultStatus');const id=lastResult.case_id,epoch=caseEpoch,b=$('#deleteCase');b.disabled=true;try{const res=await ui12Fetch('delete',`/api/v1/cases/${encodeURIComponent(id)}`,{method:'DELETE'},12000);if(!res.ok)throw new Error('Chưa xóa được kết quả trên máy chủ. Vui lòng thử lại.');if(epoch!==caseEpoch||!lastResult||lastResult.case_id!==id)return;if(localStorage.getItem(LAST_CASE_KEY)===id)localStorage.removeItem(LAST_CASE_KEY);resumeResult=null;newCheck();updateResumeCard();toast('Đã xóa kết quả này khỏi kho lưu tạm của demo.');}catch(e){if(epoch===caseEpoch)ui12Inline('#resultStatus',e.message||'Mất kết nối. Chưa xác nhận xóa kết quả.');b.disabled=false;}};
bindClarification=function(){$('#editDescription')?.addEventListener('click',()=>{show('#intake');$('#situation').focus();});$('#clarifyForm')?.addEventListener('submit',async e=>{e.preventDefault();if(!submittedInput)return;const b=e.submitter;b.disabled=true;ui12Inline('#resultStatus');const epoch=caseEpoch,source=lastResult?.case_id,answers=Object.fromEntries(new FormData(e.target));try{const res=await ui12Fetch('clarify','/api/v1/check',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({...submittedInput,exposure_answers:answers})},18000);const data=await res.json().catch(()=>({}));if(!res.ok)throw new Error(data.detail||'Không cập nhật được.');if(epoch!==caseEpoch||lastResult?.case_id!==source)return;submittedInput={...submittedInput,exposure_answers:answers};submittedCaseId=data.case_id;lastResult=data;localStorage.setItem(LAST_CASE_KEY,data.case_id);renderResult(data);}catch(err){if(epoch===caseEpoch)ui12Inline('#resultStatus',err.message||'Không cập nhật được. Dữ liệu hiện tại vẫn được giữ.');b.disabled=false;}});};

function ui12BindEntries(){document.querySelectorAll('[data-entry]').forEach(old=>{const b=old.cloneNode(true);old.replaceWith(b);b.addEventListener('click',()=>{const mode=b.dataset.entry;resetIntake();show('#intake');if(mode==='recovery')$('#situation').placeholder='Bạn đã chuyển tiền, đọc/gửi mã hay cho người khác truy cập thiết bị? Mô tả riêng từng việc; không nhập mã hoặc mật khẩu.';});});}
function ui12Init(){ui12PatchHome();ui12PatchForm();ui12BindEntries();if(!$('#resultStatus'))$('#resultMount')?.insertAdjacentHTML('beforebegin','<div id="resultStatus" class="inline-error" role="alert" hidden></div>');const dm=document.querySelector('.dialog-head');if(dm&&!document.querySelector('.dialog-note'))dm.insertAdjacentHTML('afterend','<p class="dialog-note">Màu và nhãn phản ánh ý nghĩa của từn