huống nguồn</small></div><div><strong>22</strong><small>cấu hình quyết định chuẩn hóa</small></div><div><strong>40/40</strong><small>PAR và B2 v0.2</small></div><div><strong>0/40</strong><small>khác biệt action class</small></div></div><p>Unsafe continuation: <strong>0/22 nhóm đủ điều kiện</strong>; benign unnecessary interruption: <strong>0/10</strong>. Gold suy ra từ rubric, chưa qua đánh giá độc lập; benchmark chỉ đánh giá decision layer.</p></section><section class="about-card architecture-card"><span class="eyebrow">KIẾN TRÚC PAR</span><div class="architecture-flow"><span>Bằng chứng</span><b>→</b><span>Quyết định</span><b>→</b><span>Chuyển tiếp trách nhiệm</span><b>→</b><span>Kết quả</span><b>→</b><span>Học có kiểm soát</span></div><p><strong>Authority · Provenance · Time · Policy · Trace</strong> là thuộc tính xuyên suốt. Học có kiểm soát là kiến trúc mục tiêu, chưa phải closed-loop feature đang chạy trên rc4.</p></section><section class="about-card"><span class="eyebrow">LỘ TRÌNH CÓ ĐIỀU KIỆN</span><ol class="roadmap-list"><li>Đo khả năng hiểu hành động trong 5–10 giây.</li><li>Thử nghiệm offline/shadow có giới hạn với dữ liệu được phép sử dụng.</li><li>Outcome có căn cứ → candidate knowledge → review → replay/shadow → phê duyệt phiên bản.</li><li>GO / REVISE / STOP / INSUFFICIENT_EVIDENCE theo bằng chứng.</li></ol></section></div>`;
  const oldBench=document.querySelector('.benchmark-section');if(oldBench)oldBench.replaceWith(details);else boundary.before(details);
}
function ui12PatchForm(){
  const old=$('#checkForm');if(!old)return;const form=old.cloneNode(true);old.replaceWith(form);
  const action=$('#actionState')?.closest('.field');if(action&&!$('#credentialExposure')){const fs=document.createElement('fieldset');fs.className='exposure-fieldset';fs.innerHTML=`<legend>Bạn đã thực hiện việc nào?</legend><p class="field-help">Tách riêng tiền, mã/mật khẩu và quyền truy cập để PAR không suy một loại hành động thành loại khác.</p><div class="three-col"><label class="field"><span>Với tiền</span><select id="actionState"><option value="UNKNOWN">Chưa rõ</option><option value="NOT_STARTED">Chưa thực hiện</option><option value="MOBILISED_NOT_TRANSFERRED">Đã chuẩn bị/rút nhưng chưa chuyển</option><option value="ALREADY_TRANSFERRED">Đã chuyển tiền</option></select></label><label class="field"><span>Mã / mật khẩu</span><select id="credentialExposure"><option value="UNKNOWN">Chưa rõ</option><option value="NO">Chưa gửi/đọc cho người khác</option><option value="YES">Đã gửi/đọc cho người khác</option></select></label><label class="field"><span>Quyền truy cập</span><select id="controlExposure"><option value="UNKNOWN">Chưa rõ</option><option value="NO">Chưa cấp quyền</option><option value="YES">Đã cấp quyền / điều khiển</option></select></label></div>`;action.replaceWith(fs);}
  const submit=$('#checkForm .submit');if(submit&&!$('#formError'))submit.insertAdjacentHTML('beforebegin','<div id="formError" class="inline-error" role="alert" hidden></div>');
  ['phone','account'].forEach(id=>$('#'+id)?.addEventListener('input',()=>{updateTray();submittedInput=null;}));$('#situation')?.addEventListener('input',()=>{updateCount();submittedInput=null;});['actionState','credentialExposure','controlExposure'].forEach(id=>$('#'+id)?.addEventListener('change',()=>{submittedInput=null;}));
  $('#evidenceFile')?.addEventListener('change',ui12Upload);
  $('#checkForm')?.addEventListener('submit',ui12Submit);
}
async function ui12Upload(){const file=$('#eviden