/* AEGI PAR UI 1.2 frontend-only hardening. Backend/decision engine remains rc4. */
const ui12Controllers=new Map();
function ui12Cancel(slot){const c=ui12Controllers.get(slot);if(c){c.abort();ui12Controllers.delete(slot);}}
async function ui12Fetch(slot,url,options={},timeout=15000){ui12Cancel(slot);const c=new AbortController();ui12Controllers.set(slot,c);const t=setTimeout(()=>c.abort(),timeout);try{return await fetch(url,{...options,signal:c.signal});}catch(e){if(e?.name==='AbortError')throw new Error('Yêu cầu quá thời gian hoặc đã được thay bằng thao tác mới. Vui lòng thử lại.');throw e;}finally{clearTimeout(t);if(ui12Controllers.get(slot)===c)ui12Controllers.delete(slot);}}
function ui12Inline(id,msg=''){const el=$(id);if(!el)return;el.textContent=msg;el.hidden=!msg;}
function ui12Payment(v){return v==='ALREADY_TRANSFERRED'?'YES':(['NOT_STARTED','MOBILISED_NOT_TRANSFERRED'].includes(v)?'NO':'UNKNOWN');}
function ui12Exposure(){return {payment:ui12Payment($('#actionState').value),credential:$('#credentialExposure')?.value||'UNKNOWN',control:$('#controlExposure')?.value||'UNKNOWN'};}
function ui12EvidencePresentation(e){const k=e.kind||'',s=e.state||'';if(k==='OFFICIAL_SCENARIO_WARNING'&&s==='MATCH')return ['state-adverse','Khớp cảnh báo'];if(k==='PROCEDURE_COMPARISON'&&['DEVIATION','PARTIAL_MATCH'].includes(s))return ['state-adverse','Lệch tham chiếu'];if(k==='REVIEWED_PATTERN'&&s==='SIMILAR_REVIEWED')return ['state-adverse','Tương tự mẫu đã rà soát'];if(s==='REFERENCE_MATCH')return ['state-info','Khớp tham chiếu'];if(k==='PROCEDURE_COMPARISON'&&s==='MATCH')return ['state-info','Phù hợp tham chiếu'];if(s==='QR_DECODED')return ['state-info','Đã đọc QR'];if(s==='NO_MATCH')return ['state-neutral','Chưa thấy trong phạm vi'];if(s==='UNAVAILABLE')return ['state-unknown','Nguồn chưa khả dụng'];if(s==='STALE')return ['state-unknown','Dữ liệu có thể đã cũ'];if(s==='CONFLICTED')return ['state-unknown','Thông tin mâu thuẫn'];if(['SCOPE_UNKNOWN','OUT_OF_SCOPE'].includes(s))return ['state-unknown','Ngoài/chưa rõ phạm vi'];return ['state-neutral',s||'Không rõ'];}

function ui12PatchHome(){
  const ver=document.querySelector('.brand small');if(ver)ver.textContent='Data for Life 2026 · Demo rc4 / UI 1.2';
  const quick=document.querySelector('[data-entry="quick"]');if(quick)quick.remove();
  document.querySelector('.entry-grid')?.classList.add('two-entry');
  const boundary=document.querySelector('.boundary-bar');if(!boundary||document.querySelector('#aboutPrototype'))return;
  const details=document.createElement('details');details.id='aboutPrototype';details.className='about-prototype';details.innerHTML=`<summary><span><span class="eyebrow">VỀ BẢN THỬ NGHIỆM</span><strong>Kiến trúc, bằng chứng và lộ trình</strong></span><span class="summary-hint">Mở chi tiết</span></summary><div class="about-grid"><section class="about-card"><span class="eyebrow">ĐÃ TRIỂN KHAI & KIỂM TRA</span><div class="evidence-stats"><div><strong>90/90</strong><small>rc4 candidate tests</small></div><div><strong>47/47</strong><small>source manifest</small></div><div><strong>6/6</strong><small>public HTTP gates</small></div><div><strong>3/3</strong><small>public decision flows</small></div></div><p>Bằng chứng runtime/demo trong phạm vi đã kiểm tra; không phải tuyên bố production-ready hay hiệu quả chống lừa đảo ngoài thực tế.</p></section><section class="about-card"><span class="eyebrow">ĐÁNH GIÁ NỘI BỘ</span><div class="evidence-stats"><div><strong>40</strong><small>họ tình 