g loại bằng chứng; khớp tham chiếu không phải chứng nhận an toàn.</p>');}
show=function(id){$$('.view').forEach(v=>v.classList.remove('active'));const view=$(id);view.classList.add('active');window.scrollTo({top:0,behavior:'auto'});requestAnimationFrame(()=>{const h=view.querySelector('h1,h2');if(h){h.setAttribute('tabindex','-1');h.focus({preventScroll:true});}});};
updateResumeCard=async function(){const id=localStorage.getItem(LAST_CASE_KEY),card=$('#resumeCard');if(!id){card.hidden=true;resumeResult=null;return;}try{const res=await ui12Fetch('resume',`/api/v1/cases/${encodeURIComponent(id)}`,{},10000);if(id!==localStorage.getItem(LAST_CASE_KEY))return;if(res.status===404){localStorage.removeItem(LAST_CASE_KEY);card.hidden=true;resumeResult=null;return;}if(!res.ok)throw new Error();resumeResult=await res.json();card.hidden=false;}catch{if(id===localStorage.getItem(LAST_CASE_KEY)){card.hidden=true;resumeResult=null;}}};
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',ui12Init);else ui12Init();
